# GAPSCOPE — Project Instructions

This file is the starting reference for each Claude conversation. It captures the current state of the project so future chats have context. **Update it at the end of every working session.**

## What this project is

GAPSCOPE is a **capture integrity inspector**: a single-file, browser-based tool that analyzes timestamped data captures for timing anomalies — dropped samples, dropout locations, and arrivals that are too close together. It reads CSV/text captures, Harp `.bin` binary captures, and whole **experiment folders** (one sub-folder per device). It runs entirely client-side — no uploads, no server, no build step.

The whole application lives in **one file**: [index.html](index.html) (HTML + CSS + vanilla JS, no dependencies). Two Node harnesses, [test_experiment.js](test_experiment.js) (unit-level) and [test_flow.js](test_flow.js) (end-to-end over the sample experiment), run the page's real script with DOM stubs — `node test_experiment.js && node test_flow.js`.

Sample data in the repo: `2026-06-12T010203Z/` (a real experiment folder: 13 device dirs, root config files) and `devices/` (vendored known-device schemas, `<device>.device.yml`).

## Core concepts

- **Stream-type classification** — every stream is classed `pontual | constant | burst` by `classifyStream(gaps, Δt, tol, n, spanNs)` (built on `streamRegularity`). **Pontual**: on-beat fraction (gaps within `Δt/tol … Δt·tol`) `< CLASS_P_MIN` (0.65) — no dominant cadence, so Δt/missing are suppressed entirely. Otherwise **coverage** `= n·Δt / spanNs` decides: `> CLASS_COVER_MIN` (0.5) ⇒ **constant** (today's model — every gap > tol·Δt is a dropout), else ⇒ **burst** (regular cadence over only part of the timeline). For burst, missing is counted **only within runs**: in-data inter-burst silences (a bimodal gap-distribution jump ≥ `CLASS_JUMP`=8× above a `CLASS_BREAK_MULT`=12×Δt floor, `silenceNs`) are excluded, and the empty timeline before/after the data isn't a gap. `streamType` (per-stream override: auto/pontual/constant/burst, reset on load) vs `streamClass` (resolved) vs `autoClass` (what auto decided). Shown as a color-coded chip (C/B/P) on cards and the stream selector.
- **Experiment timeline** — devices run concurrently, so a single stream is shown/classified against the **whole experiment span**, not its own. `computeTimeline` reads each stream's first+last timestamp (bin: header `firstTs` + `readHarpLastTs` tail read; CSV: `readCsvBounds` head/tail) → `experiment.timeline = {startNs, endNs, durationNs}` (epoch-shifted ns). On load, `refOriginNs` anchors `offsets` to `startNs` (so `offsets[0]` ≥ 0 — the lead-in), and `timelineNs` is the full view/classification span; flat (non-experiment) loads keep own-span behavior (`refOriginNs=null`). `analyze` separates `dataSpanNs` (active span, for rate/constant-expected) from `durationNs`=`timelineNs` (the view). Empty timeline (before/after data, or inside a silence) renders as a dark "no data" heatmap band + a trace note.
- **Δt (nominal interval)** — the expected spacing between samples. Auto-detected per load by `detectInterval`: the **median gap refined to the mean of the gaps within ±50% of it**. The trim makes it tick-aware — Harp timestamps tick every 32 µs, so a true 1 ms register yields only 992/1024 µs gaps (~3:1) and the bare median locks onto 992 while the trimmed mean lands on the true 1000 (fixing a spurious ~0.8% "Missing" on perfect captures); dropout gaps and tight doublets fall outside the window, so the estimate keeps the median's robustness. **Δt is never persisted** — every stream re-detects its own (registers run at different rates); a manually typed+applied Δt holds only for the capture on screen.
- **Gap threshold** — multiplier of Δt above which a gap counts as a dropout (default 1.5×).
- **Too-close threshold** — a **multiplier of Δt** (default 0.9×) below which a spacing is flagged as suspiciously tight. Source of truth is `closeFactor` (persisted); the ns value the analyzer/renderers consume (`closeNs = closeFactor × intervalNs`) is recomputed at every load, Apply, and Δt auto-detect.
- **Time modes** — absolute (epoch/wall-clock UTC) vs. relative (elapsed seconds), auto-detected: a first sample beyond ~1973 in epoch terms is treated as absolute. **Harp-epoch CSVs**: a numeric-seconds first value past ~3.0e9 s (≈1999 in 1904 terms ≈2065 as Unix) is treated as seconds-since-1904 and shifted to the Unix epoch (`HARP_EPOCH_OFFSET_NS`), mirroring the binary parser's rule — camera CSVs read correct 2026 wall-clock. Offsets stay relative to the raw first value.
- **Nanosecond precision** — timestamps are parsed to exact ns using BigInt; offsets from the first sample are stored as Numbers.
- **Trace layers** — three independent toggles drive the trace canvas: `showRaw` (per-channel value plots), `showAnom` (dropout/too-close spikes + heatmap + tables), and `showTs` (a vertical line per sample). They compose; `showTs` lines are drawn under the anomaly/raw layers and collapse to one line per pixel column via the same `columns()` aggregation the spikes use.
- **Measurement arrows** — on hover, double-headed arrows on the overlay canvas show the Δt between the items bracketing the cursor: between events (when `showAnom`, accent blue) and/or between samples (when `showTs`, steel blue).
- **Per-missing-message rendering** — a dropout is drawn as one line per missing message at its expected arrival time (`gap.start + k·Δt`), colored/height-scaled by the parent gap's severity; below ~2px expected spacing the burst collapses to one spike per pixel column.
- **Settings persistence** — `tolerance`, `closeFactor`, the three view toggles, and the chunk size are saved to `localStorage` (`gapscope.settings`). Legacy keys (`closeNs` in µs, pinned `intervalNs`) are ignored on load. The **auto** button re-detects Δt on demand.
- **Zoom depth** — tightest window is `intervalNs * 5` (~5 samples), uniform regardless of capture size.
- **Chunked streaming load** — files are read as 8 MB (tunable, `#chunk`) byte slices via `file.slice` + `FileReader.readAsArrayBuffer`, decoded with a streaming `TextDecoder` carrying the partial trailing line, fed line-by-line into `makeLineParser().feed()`. Max file size is bounded by sample-count memory, not raw bytes. The loaded meta line reports a `loaded in …` time.
- **Harp binary format** — `.bin` files are a flat stream of fixed-stride messages `[Type, Length, Address, Port, PayloadType, (Timestamp), Payload…, Checksum]`; `stride = Length + 2`, `PayloadType` carries the dtype code with bit `0x10` set when a timestamp is present, timestamp is `uint32 s + uint16 32µs-ticks` since **1904-01-01**. A single `.bin` holds one register; stride/dtype/channels lock in from the first message. `makeHarpParser` fills the **same globals** as `makeLineParser`, so analyze/pyramid/render are format-agnostic. Clock-synced captures (first second past ~1999) get the 1904→1970 shift. Format chosen by `detectBinary` (extension, else printable-byte sniff).
- **Register snapshot dumps** — a `.bin` whose messages span *different* addresses (e.g. `Behavior0_<ts>.bin`, no register number) is the per-register device state written at connect: one message per register, variable stride. Detected in `readHarpHeader`'s pre-scan (second message address differs); excluded from register grouping; parsed whole by `parseHarpSnapshot` (variable-stride walk) for the **register inventory** and **WhoAmI** (register 0) — used to identify the device.
- **Known-device schema library** — dropped `device.yml` schemas (any `.yml` that parses to `device`/`whoAmI`/`registers`) join `deviceSchemas` keyed by device name and are **persisted as raw yml text** in `localStorage["gapscope.devices"]` — drop the `devices/` folder once and it's remembered across sessions. File-name fallback (when `device:` is absent) honors the `<device>.device.yml` convention. The drop zone shows "N known device schemas … forget". `matchSchema(name, whoAmI)`: exact **WhoAmI** match first (from a snapshot dump), else the **longest case-insensitive device-name prefix** of the folder/file name (`Behavior` → `Behavior0`). `harpSchema` is the schema applied to the *current* load ({device, whoAmI, registers:{addr→{name,members}}}); a schema dropped on its own also becomes active (old sticky behavior). Ymls that don't parse as device schemas (e.g. a rig configuration) are treated as data/config, never as schemas; `.md`/`.json` riding along a flat drop are inert.
- **Folder classification** — every dropped/picked file carries a relative path (`webkitRelativePath` from the picker; drag traversal stamps `entry.fullPath` as `_relPath`; bare files have none). `splitByFolders` strips the dropped folder itself (shared first segment), then: files in sub-folders → **experiment** (devices keyed by first segment; root-level files stashed as `configFiles` — `Metadata.json`, `RigConfiguration_*.yaml`, currently unused but kept for the rig-mapping feature); all files at one level → **flat** (the original single-device behavior: bins grouped by register, or one CSV).
- **Experiment mode** — `experiment = {name, configFiles, devices[], timeline}`; a device is `{name, binFiles, csvs, other, bytes, byAddr, addresses, snapshot, schema, streams, _card}`. On drop, every device's bins are header-scanned (first KB: register grouping + snapshot detection) and snapshots parsed; then `computeTimeline` measures the shared experiment span before the cards render. The **device card grid** shows: name, schema badge (matched name or "unknown device"), snapshot register count + WhoAmI, sizes, and a per-stream table (**type chip** / msgs / Δt / missing) that fills in as the background scan completes. Clicking anywhere on a card opens the largest-by-bytes stream; clicking a specific **register row** (`tr[data-i]`, hover-highlighted) opens that stream directly — `openDevice(dev, idx)`. A **"← devices"** back button returns; "Load another capture" or a flat load closes the experiment; schema imports while it's open re-match unknown devices live.
- **Streams** — the loadable units of a selection: one per Harp register (its files concatenated in timestamp order through a single parser → one continuous timeline) plus one per CSV file. The loaded-bar selector (`#regsel`) is a **custom dropdown** (native `<select>` can't render the colored type chip): a button showing the current stream's chip+label and a popup listing every stream with its type chip on the left (`refreshStreamSelector`, rebuilt at the end of every `render` so the chip tracks the resolved type/override). Shows when there are ≥2 streams; default is the largest by bytes. Flat multi-bin drops use the same machinery (`buildStreams`/`loadStream`).
- **Background summary scan** — after the device list renders, a sequential queue walks every stream with **timestamp-only** parsers (`makeHarpStatsWalker`/`makeCsvStatsWalker` → `makeStatsAcc(spanNs)`): first 65,536 gaps buffered → `detectInterval` + `streamRegularity` → every gap judged against `tolerance × Δt`, tracking the full and silence-attributable subsets; `done()` classifies via coverage (`count·Δt / experiment span`) and yields {count, dtNs, drops, missing, type} (burst figures exclude in-data silences) with O(1) memory, never touching the loaded capture's globals. **Paused** while a device is open (in-flight chunk parks its continuation on `scanState.cont`), resumed on back, cancelled on close (epoch counter orphans in-flight reads). `#scanline` shows progress. Card numbers are estimates (Δt from the first 65k gaps, tolerance as of scan time) — opening the device computes exact figures.
- **Big-capture render perf** — per-frame cost proportional to what's on screen: `columns`/`channelColumns` bind to visible samples via `lowerBound`; the full-capture heatmap columns are cached (`heatCache`/`heatEpoch`); dropout loops use `visibleGapRange` (binary search over `gapEvents`).
- **LOD aggregate pyramid** — `buildPyramid()` (once per load) precomputes multi-resolution clusters (finest bins `LOD_BASE`=64 raw samples; coarser levels merge pairs). `pickLevel` chooses the coarsest level with `size ≤ samples/pixel`; `columns`/`channelColumns` aggregate from it via `firstBin`, making zoomed-out draws O(cols). Heatmap stays pixel-exact (`forceRaw`). Built when `N ≥ LOD_BASE*4`.
- **Trace navigation** — scroll = wheel-zoom at cursor; **left-drag = zoom-select**; **right-drag = pan**; double-click = reset. The heatmap minimap has its own click/drag navigation.
- **Known browser limit** — folder *drag-drop* traversal is blocked by Chromium on `file://` pages; a folder drag from disk errors with a message pointing to the **"pick a folder"** button (not restricted) or to serving over `http://localhost`.

## File / code map (index.html)

- **CSS** (`:root` theme vars, dark "phosphor" oscilloscope aesthetic) — lines ~7–252. Includes the experiment list / device card styles (`.explist`, `.devgrid`, `.devcard`, badges, `.scanline`, `.schemastat`).
- **Markup** — drop zone (+ `#schemastat` schema-library line), **experiment list** (`#explist`: header, `#scanline`, `#devgrid`), loaded bar (+ `#backexp`, stream selector `#regsel`), progress, results — lines ~254–443.
- **JS** (IIFE, `"use strict"`) — lines ~444 onward:
  - **State** — capture globals (offsets, gaps, channels, stats, view window) plus `harpSchema` (active schema), `deviceSchemas` (library), `streamList`/`streamIdx`, `experiment`/`currentDevice`/`scanState`, and classification/timeline state (`streamType`/`streamClass`/`autoClass`, `silenceEvents`, `refOriginNs`, `timelineNs`).
  - **Parsing** — `parseTsNs` (timestamp → BigInt ns), `makeLineParser` (per-line feeder; 1904-epoch shift for Harp-seconds CSVs; offsets anchored to `refOriginNs` when set), `streamParse` (chunked text reader), `median`, `detectInterval` (tick-aware Δt estimate).
  - **Classification** — `streamRegularity` (on-beat fraction + bimodal silence threshold), `classifyStream` (regularity gate → coverage split), constants `CLASS_P_MIN`/`CLASS_COVER_MIN`/`CLASS_JUMP`/`CLASS_BREAK_MULT`.
  - **Harp binary** — `harpDtype`, `concatBytes`, `makeHarpParser` (fixed-stride walker → same globals; `refOriginNs` anchor), `streamParseBinaryFiles`/`streamParseBinary`, `readHarpHeader` (first-KB pre-scan: address, first ts, `stride`, **snapshot detection**), `parseHarpSnapshot` (variable-stride dump walk → registers + WhoAmI), `shiftTsNs`/`readHarpLastTs`/`readCsvBounds`/`streamTimeBounds`/`computeTimeline` (experiment timeline bounds), `relPathOf`/`entryToFiles`/`traverseEntries` (path-stamped folder traversal), `parseHarpYaml` (anchor/merge-aware; also captures `device:`/`whoAmI:`), `detectBinary`.
  - **Schema library** — `saveDeviceSchemas`/`loadDeviceSchemas`/`clearDeviceSchemas` (raw-text persistence), `importSchemas` (drop → parse → library, `<device>.device.yml` fallback naming), `matchSchema` (WhoAmI, then longest name prefix), `refreshSchemaStat` (drop-zone line).
  - **Analysis** — `analyze` classifies (auto/override), builds `gapEvents`, `tightEvents`, `silenceEvents`, merged `eventList`, and `stats` (incl. `type`/`coverage`/`dataSpanNs`/`silenceNs`); `visibleGapRange`.
  - **LOD pyramid** — `buildPyramid`/`newLevel`, `pickLevel`, `firstBin`.
  - **Formatting** — `fmtGap`, `fmtDuration`, `absClock`, `absDate`, etc.
  - **Canvas** — `columns`/`channelColumns`, `drawTrace` (silence bands + no-data note), `drawSilenceBands`, `drawAnomBipolar`, `drawRawChannels`, `overlayAnomBands`, `drawAllTimestamps`, `drawHeat` (no-data/silence/dropout 3-way coloring), `drawMeasureArrows`, `drawSelection`.
  - **Render** — `render` (type-aware stat grid + verdict; calls `refreshStreamSelector`), `renderTable`, `renderTightTable`, `buildChannelLegend`, `applyViewVisibility` (hides dropouts table for pontual).
  - **File handling** — `returnHome` (error/back → experiment list or drop zone), `handleFiles` (schema import → `splitByFolders` → experiment / flat dispatch), `handleHarpBins` (flat bin sets: scan, group, WhoAmI ident), `buildStreams`/`largestStream`/`streamLabel`, `streamTypeChip`/`refreshStreamSelector` (custom chip dropdown), `loadStream`, `finishLoad` (per-load Δt re-detect + `timelineNs` + `closeNs` derivation), `handleFile`/`loadSingleFile`.
  - **Experiment mode** — `startExperiment` (structure scan → `computeTimeline` → device build), `teardownExperiment`, `renderExperiment`/`fillDevCard` (type column, clickable `tr[data-i]` rows), `openDevice(dev, idx)`.
  - **Background scan** — `makeStatsAcc`, `makeHarpStatsWalker`, `makeCsvStatsWalker`, `scanStream` (pause-aware chunked reader), `startScan`/`pumpScan`/`pauseScan`/`resumeScan`/`cancelScan`, `updateScanLine`.
  - **Interaction** — `bindHover`, drop/picker wiring, `#backexp`/`#expclose` handlers, apply/auto/toggle handlers, settings (`saveSettings`/`loadSettings`/`reflectSettings`), resize.

## Current state

- Fully functional single-file tool; all prior features (streaming loads, LOD pyramid, worst-case heatmap, navigation, Harp single/multi-file support, experiment layer, schema library) intact.
- **This session added stream-type classification + the shared experiment timeline** (core concepts above). Streams are auto-classed `pontual / constant / burst` by **coverage** of the experiment timeline (`n·Δt / span`, >50% ⇒ constant; no cadence ⇒ pontual), with a per-stream override (`#streamtype`). Missing is suppressed for pontual and counted only within runs for burst (in-data silences via a bimodal jump are excluded). Each device's streams are anchored to a **shared experiment timeline** (`computeTimeline` from per-stream first/last timestamps) so a single register is shown/classified against the whole experiment, with idle stretches drawn as dark "no data" bands. `analyze` separates `dataSpanNs` from the view `timelineNs`; rate uses the active span.
- Also this session (UI): the stream type shows as a **color-coded chip** (C/B/P; burst = accent blue, constant = phosphor, pontual = dim) in its own **type column** on the cards and in a **custom dropdown** (`#regsel` replaced the native `<select>` so the chip can render — button + popup with the chip left of each label); **register rows are individually clickable** (`tr[data-i]`, hover-highlighted) to open that stream, vs. clicking elsewhere on the card for the largest; device cards widened (`minmax(512px)`) and the page uses full window width.
- **Verification**: `node test_experiment.js` (unit: yaml/matching/snapshot/stats/**classification incl. coverage + silence exclusion**/epoch-shift/Δt estimator) and `node test_flow.js` (end-to-end on the sample experiment: 13 devices, schema matching, **timeline anchoring** — `firstNs` is now the experiment origin — scan incl. mid-chunk pause/park/resume + type, device open with schema channel names, threshold derivation). Both green. In-browser UI verified by the user on `http://localhost`.
- **Residual ambiguity (by design):** a constant stream that loses >50% of its messages (so coverage drops below 50%) classifies as burst, hiding that loss from the missing count — the now-visible no-data band + the per-stream override cover it. Timing alone can't separate a long dropout from an inter-burst silence.
- Known matching limitation (by design, next feature): in the sample experiment only `Behavior0` matches a schema — `CameraController`, `ClockSynchronizer`, `Feeder*`, `Loom*` are *role* names that don't prefix their device-type names (`CameraControllerGen2`, `TimestampGeneratorGen3`, `OutputExpander`, `FastStepper`) and have no snapshot dump for a WhoAmI match.

## Conventions / preferences

- Keep it dependency-free and single-file unless there's a strong reason not to.
- Vanilla JS, ES5-ish style (`var`, function expressions) consistent with the existing code.
- When asked about a bug/feature, answer first and confirm before changing anything (see CLAUDE.md).
- Remind the user to push to GitHub before big structural changes.
- Run both Node test harnesses after parser/flow changes.

## Ideas / possible future work

- **RigConfiguration mapping (NEXT — agreed with the user)** — parse the experiment's `RigConfiguration_*.yaml` / `Metadata.json` (already stashed in `experiment.configFiles`) to map *role* folder names (LoomEast, CameraController, Feeder0…) to device types, so schema matching covers devices that neither prefix-match nor carry a snapshot WhoAmI.
- **Cross-register merge** — merge *different* registers onto a union timeline as separate channels (accepting that global gap analysis then mixes rates).
- **Schema/data mismatch note** — surface a small UI warning when a register's declared `device.yml` member count differs from the channel count found in the `.bin`.
- **`device.yml` `$ref`/`!include`** — follow external schema references (e.g. `common.yml`) that the minimal YAML reader skips.
- **Re-scan on threshold change** — device-card stats keep the tolerance from scan time; changing the Gap threshold could invalidate/re-run the summary scan.
