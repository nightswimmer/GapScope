# GAPSCOPE — Project Instructions

This file is the starting reference for each Claude conversation. It captures the current state of the project so future chats have context. **Update it at the end of every working session.**

## What this project is

GAPSCOPE is a **capture integrity inspector**: a single-file, browser-based tool that analyzes timestamped data captures (CSV/text) for timing anomalies. The user drops a file and GAPSCOPE reports dropped samples, dropout locations, and arrivals that are too close together. It runs entirely client-side — no uploads, no server, no build step.

The whole application lives in **one file**: [index.html](index.html) (HTML + CSS + vanilla JS, no dependencies).

## Core concepts

- **Δt (nominal interval)** — the expected spacing between samples. Auto-detected from the median gap, or set manually.
- **Gap threshold** — multiplier of Δt above which a gap counts as a dropout (default 1.5×).
- **Too-close threshold** — a floor (default 900 µs) below which a spacing is flagged as suspiciously tight.
- **Time modes** — absolute (epoch/wall-clock UTC) vs. relative (elapsed seconds), auto-detected: a first sample beyond ~1973 in epoch terms is treated as absolute.
- **Nanosecond precision** — timestamps are parsed to exact ns using BigInt; offsets from the first sample are stored as Numbers.
- **Trace layers** — three independent toggles drive the trace canvas: `showRaw` (per-channel value plots), `showAnom` (dropout/too-close spikes + heatmap + tables), and `showTs` (a vertical line per sample). They compose; `showTs` lines are drawn under the anomaly/raw layers and collapse to one line per pixel column via the same `columns()` aggregation the spikes use.
- **Measurement arrows** — on hover, double-headed arrows on the overlay canvas show the Δt between the items bracketing the cursor: between events (when `showAnom`, accent blue) and/or between samples (when `showTs`, steel blue). Both can show at once, offset vertically.
- **Per-missing-message rendering** — a dropout is drawn as one line per missing message at its expected arrival time (`gap.start + k·Δt`), not as a single band spanning the gap. Lines are colored/height-scaled by the parent gap's severity (`spikeColor` + log scale). Below ~2px expected spacing the burst collapses to one spike per pixel column. Applies to both `drawAnomBipolar` and the raw-mode `overlayAnomBands`.
- **Settings persistence** — `tolerance`, `closeNs`, the three toggles, and a *pinned* Δt are saved to `localStorage` (`gapscope.settings`) and restored on load (`saveSettings`/`loadSettings`/`reflectSettings`). Δt auto-detects when no pin is saved; the **auto** button re-detects on demand and clears the pin.
- **Zoom depth** — tightest window is `intervalNs * 5` (~5 samples), uniform regardless of capture size (no duration-relative floor).
- **Chunked streaming load** — files are read as 8 MB (tunable) byte slices via `file.slice` + `FileReader.readAsArrayBuffer`, decoded with a streaming `TextDecoder` that carries an incomplete trailing line (`tail`) across chunk boundaries, and fed line-by-line into `makeLineParser().feed()`. This avoids both the V8 ~512 MB single-string cap (`readAsText`) and a giant lines array, so max file size is now bounded by sample-count memory, not raw bytes. The **Read chunk** control (`#chunk`, MB) is persisted in settings and read at the start of each load. The loaded-file meta line reports a `loaded in <ms/s>` read+parse time (measured from `performance.now()` at `handleFile` start) for benchmarking.
- **Harp binary format** — GapScope also reads [Harp](https://harp-tech.org) `.bin` capture files (the binary format used by harp-python). A Harp file is a flat stream of fixed-stride messages `[Type, Length, Address, Port, PayloadType, (Timestamp), Payload…, Checksum]`; `stride = Length + 2`, `PayloadType` carries the dtype code with bit `0x10` set when a timestamp is present, and the timestamp is `uint32 seconds + uint16 32µs-ticks` since **1904-01-01**. Because a single `.bin` holds one register, the stride/dtype/channel-count are locked in from the first message and the rest are walked at that fixed stride (mirroring harp-python). `makeHarpParser` fills the **same globals** as `makeLineParser` (`offsets`/`gaps`/`firstNs`/`channels`/counters), so everything downstream (analyze, pyramid, render) is format-agnostic — no other code path changed. Timestamps decode to BigInt ns via `sec·1e9 + ticks·32000`; for a clock-synced capture (first second past ~1999) the 1904→1970 offset (`2082844800` s) is **subtracted** so absolute/wall-clock mode reads correctly, while boot-relative captures stay small and fall into relative-seconds mode. Format is chosen by `detectBinary` (`.bin`/`.harp` → binary, `.csv`/`.txt` → text, else a printable-byte sniff of the first 256 bytes). Streaming reuses the same chunked `ArrayBuffer` reader as text (`streamParseBinary`), carrying a partial trailing message (`< stride` bytes) across chunk boundaries.
- **Harp multi-file combine** — multiple Harp `.bin` files can be loaded at once (multi-select, or by picking/dropping a **folder** — recursive `webkitGetAsEntry` traversal for drag, `webkitdirectory` input for the picker). `readHarpHeader` pre-scans just the first message of each file (register address + first timestamp), files are grouped by **register address**, and each group is concatenated **in timestamp order through a single parser** (`streamParseBinaryFiles`) into one continuous timeline — so a session split across files becomes one stream and gap analysis spans the whole recording (a real gap between files shows as a genuine dropout; a seamless split shows none). Because Harp timestamps are absolute, ordering/stitching is exact. If the set spans more than one register (e.g. a whole device folder), the largest register loads by default and a **register selector** (`#regsel`) in the loaded bar switches which one is analyzed (`loadHarpRegister`). `handleFiles` classifies the dropped/picked set into schema (`.yml`) / binary (`.bin`) / other and dispatches; single `.bin` and CSV paths are unchanged (`streamParseBinary` is now a one-element wrapper of `streamParseBinaryFiles`). Grouping is by address only — a register whose stride changed mid-session would mis-walk (not a real-world concern). **Drag-drop handling**: the whole `window` is the drop target (`dispatchDrop` runs from the window `drop` listener, both before and after a capture loads, so margin drops work; the box only toggles the `.over` highlight). A drop that yields no usable files surfaces a visible `showError` instead of failing silently. **Known browser limit**: folder *drag-drop* traversal (`webkitGetAsEntry`/`readEntries`) is blocked by Chromium on `file://` pages — the directory entry is returned but its contents can't be read, so a folder drag from disk errors with a message pointing to the **"pick a folder"** button (the `webkitdirectory` picker is *not* restricted on `file://`) or to serving over `http://localhost`. The picker's `dirInput.click()` is dispatched on a hidden input inside `#drop`; the `#drop` click handler ignores clicks whose target is `fileInput`/`dirInput`/`#pickfolder` so opening the folder picker doesn't also pop the file picker.
- **Harp channel labels (device.yml)** — an optional Harp `device.yml` schema names the channels. `parseHarpYaml` is a minimal indentation-based reader that handles the YAML **anchors** (`Name: &foo`) and **merge keys** (`<<: *foo`) real Harp schemas use, resolving inherited `payloadSpec` member names (memoized + cycle-guarded). It maps **register address → {name, members}** (keyed by address, read from the `.bin` itself), so the schema need not correspond 1:1 to the file. Channels are named from `payloadSpec` members; anything unresolved falls back to `reg<addr>_<i>`. The schema is parsed once and **kept across loads** (`harpSchema`), so one `device.yml` labels successive per-register `.bin` files. Known limit: `$ref`/`!include` to external files (e.g. `common.yml`) is not followed. If a register's declared member count is fewer than the channels actually present in the `.bin` (e.g. firmware-3.3 `AnalogData` declares 2 but a capture carries 3), the extra channels fall back to generic names.
- **Big-capture render perf** — per-frame cost is kept proportional to what's on screen, not total dataset size: the trace's `columns`/`channelColumns` bind to visible samples via `lowerBound`; the full-capture heatmap `columns(w,0,full,true)` is cached (`heatCache`, keyed by a `heatEpoch` data counter bumped in `resetData` + canvas width) so pan/zoom only recolors (O(width)) and redraws the viewport box; and the dropout loops in `drawAnomBipolar`/`overlayAnomBands` use `visibleGapRange(t0,t1)` (binary search over the start/end-sorted `gapEvents`) to iterate only on-screen dropouts.
- **LOD aggregate pyramid** — `buildPyramid()` (run once per load, after parse, behind a "Building overview…" status) precomputes multi-resolution clusters: the finest level bins `LOD_BASE` (64) raw samples; each coarser level merges pairs until ≤2 bins. Per bin it stores start time, `maxGap`/`minGap` (each bin owns `gaps[a..e-1]` so boundary dropouts are caught), present-count, and per-channel `cmin`/`cmax`. `pickLevel(visibleSamples,cols)` returns the coarsest level with `size ≤ samples/pixel` (or `null` → raw scan when <64 samples/px or no pyramid). `columns`/`channelColumns` aggregate from the chosen level via `firstBin` (binary search), making zoomed-out draws O(cols) instead of O(visible raw samples). Gaps spread across their time extent (`[bin.t, nextBin.t]`); samples/channel envelopes bucket at the bin's start column. Memory ~N/32. The heatmap passes `forceRaw=true` to stay pixel-exact (it's cached). Known approximation: at LOD zoom, cluster placement within a pixel is by bin-start time, so a *very* wide dropout in the trace may under-fill its pixel extent (the heatmap still shows it exactly); precision returns on zoom-in to the raw path. Pyramid only built when `N ≥ LOD_BASE*4`; cleared in `resetData`.
- **Trace navigation** — scroll = wheel-zoom anchored at cursor; **left-drag = select a region to zoom into** (shaded phosphor band + span label drawn via `drawSelection` on the overlay; drags < 4px ignored; selections narrower than `minWindowNs` expand to that floor around the midpoint); **right-drag = pan** (with `contextmenu` suppressed on the trace canvas); double-click = reset to full extent. The heatmap minimap keeps its own left-click/drag navigation.

## File / code map (index.html)

- **CSS** (`:root` theme vars, dark "phosphor" oscilloscope aesthetic) — lines ~7–207.
- **Markup** — drop zone, loaded bar, progress, results (verdict, stat grid, trace, heatmap, dropout table, tight-spacing table) — lines ~209–372.
- **JS** (IIFE, `"use strict"`) — lines ~374 onward:
  - **State** — globals for offsets, gaps, channels, stats, view window.
  - **Parsing** — `parseTsNs` (timestamp → BigInt ns), `makeLineParser` (stateful per-line feeder), `streamParse` (chunked byte-slice reader + streaming decode), `median`.
  - **Harp binary** — `harpDtype` (payload-type code → sized little-endian reader), `concatBytes` (carry helper), `makeHarpParser` (fixed-stride message walker → same globals as `makeLineParser`), `streamParseBinaryFiles` (chunked reader over an ordered file list through one parser) + `streamParseBinary` (single-file wrapper), `readHarpHeader` (first-message pre-scan for address + first timestamp), `entryToFiles`/`traverseEntries` (folder-drop recursion), `parseHarpYaml` (anchor/merge-aware `device.yml` → address→{name,members}), `detectBinary` (format sniff). Dispatched from `handleFiles` (classifies the set into schema/binary/other) → `handleHarpBins` (scan + group by register) → `loadHarpRegister` (concatenate one register) / `handleFile` (single file, branches CSV vs binary); shared post-parse in `finishLoad`; `refreshRegSelector` drives the register dropdown.
  - **Analysis** — `analyze` builds `gapEvents`, `tightEvents`, merged `eventList`, and `stats`; `visibleGapRange` (binary-search the on-screen dropout slice).
  - **LOD pyramid** — `buildPyramid` / `newLevel` (precompute multi-resolution clusters), `pickLevel` (choose level by samples-per-pixel), `firstBin` (binary-search bins by time).
  - **Formatting** — `fmtGap`, `fmtDuration` (scales ms → s → `m s` → `h m s` → `d h m`, switching to a whole-unit breakdown at/over an hour), `absClock`, `absDate`, `absSecNum`, etc.
  - **Canvas** — `columns` / `channelColumns` (per-pixel aggregation, raw or LOD-sourced), `drawTrace`, `drawAnomBipolar`, `drawRawChannels`, `overlayAnomBands`, `drawAllTimestamps`, `drawHeat` (cached + worst-case coloured), `drawOneArrow` / `drawMeasureArrows`, `drawSelection` (zoom-select band).
  - **Render** — `render`, `renderTable`, `renderTightTable`, `buildChannelLegend`, `applyViewVisibility`.
  - **Interaction** — `bindHover` (hover readouts, measurement arrows, wheel zoom, drag pan, heatmap navigation), file handling, apply/reset/toggle handlers, resize.

## Current state

- Fully functional single-file tool.
- Initial commit done. Subsequent work added the **Show All Timestamps** trace layer (`showTs`) with per-sample vertical lines and inter-sample measurement arrows (dual arrows when anomalies are also shown).
- A prior session added: **localStorage settings persistence** + an **auto-detect Δt** button, **uniform zoom depth** regardless of capture size, and **per-missing-message dropout rendering** (one severity-colored line per missing message at its expected time, replacing the spanning band).
- A prior session added: **chunked streaming file load** (`makeLineParser` + `streamParse`) replacing the `readAsText`-then-`split` path, lifting the ~512 MB single-string ceiling so multi-GB captures open; a **Read chunk (MB)** tunable + persisted setting; a **`loaded in …` read-time readout** on the file meta line for benchmarking; and **new trace navigation** — left-drag selects a region to zoom into, right-drag pans (browser context menu suppressed).
- A prior session added: **worst-case heatmap colouring** (each slice takes its most-severe dropout colour via `spikeColor`, matching the trace's severity legend; removed the old `coverageColor`/`lerp`); a **wide-dropout fill fix** (`columns` spreads each gap across every column it covers, so bursts no longer render as one line with empty space until you zoom in); and a **big-dataset performance pass** — cached full-capture heatmap columns (`heatCache`/`heatEpoch`), binary-searched dropout drawing (`visibleGapRange`), and a **multi-resolution LOD pyramid** (`buildPyramid`/`pickLevel`/`firstBin`) so `columns`/`channelColumns` draw from precomputed clusters when zoomed out — keeping pan/zoom smooth on tens of millions of samples.
- A prior session added: **Harp binary format support** — GapScope now reads Harp `.bin` capture files alongside CSV. A new fixed-stride binary parser (`makeHarpParser`/`streamParseBinary`) decodes the message stream and timestamps (since-1904, with a synced-clock epoch shift) into the same internal arrays the CSV path produces, so all analysis/rendering is unchanged. Format is auto-detected (`detectBinary`); the drop UX accepts the `.bin` plus an optional Harp `device.yml` (dropped together or beforehand — the schema is remembered across loads). `parseHarpYaml` reads the schema (handling YAML anchors and `<<:` merge keys) to label channels by register `payloadSpec`, falling back to generic `reg<addr>_<i>` names. Verified the byte-level math, chunk-boundary carry, and the anchor/merge YAML resolution in Node against a synthetic file and a real Behavior `device.yml`.
- A prior session added: **Harp multi-file combine** — load many `.bin` files (multi-select or a whole **folder**, by picker or drag); files are header-scanned, grouped by register address, and same-register files are concatenated in timestamp order through a single parser into one continuous timeline (`readHarpHeader`/`streamParseBinaryFiles`/`handleHarpBins`/`loadHarpRegister`). A **register selector** in the loaded bar switches which register is analyzed when the set spans several. `handleFiles` was reworked to classify schema/binary/other; `finishLoad` generalized to a (name, size, extra) signature. Verified the multi-file concatenation (incl. out-of-order inputs re-sorted by timestamp, no spurious file-boundary gap) with the real parser in Node; the in-browser folder-picker / drag-traversal / selector UI is not yet manually verified.
- This session fixed the folder-loading UX and duration formatting: (1) the **"pick a folder" button no longer also opens the file picker** — the `#drop` click handler ignores synthetic clicks bubbling up from the hidden `fileInput`/`dirInput` and from the `#pickfolder` link; (2) **drops now register across the whole window** before a capture loads too (was box-only pre-load), via a single window `drop` listener (`dispatchDrop`), with `dropEffect = "copy"` set on dragover; (3) a folder drop that yields **no readable files now shows a visible error** instead of failing silently — explaining that folder *drag-drop* is blocked on `file://` and pointing to the **"pick a folder"** button or serving over `http://localhost` (folder drag works there); and (4) **`fmtDuration` now scales past minutes** into `h m s` and `d h m` for long captures (whole-unit breakdown at/over an hour). Confirmed by the user that the picker-button path loads folders and the drag path errors with the new message on `file://` (the expected browser restriction).

## Conventions / preferences

- Keep it dependency-free and single-file unless there's a strong reason not to.
- Vanilla JS, ES5-ish style (`var`, function expressions) consistent with the existing code.
- When asked about a bug/feature, answer first and confirm before changing anything (see CLAUDE.md).
- Remind the user to push to GitHub before big structural changes.

## Ideas / possible future work

- **Cross-register merge** — current multi-file support concatenates one register at a time (with a switcher). A different mode could merge *different* registers onto a union timeline as separate channels (accepting that global gap analysis then mixes rates).
- **Schema/data mismatch note** — surface a small UI warning when a register's declared `device.yml` member count differs from the channel count found in the `.bin`.
- **`device.yml` `$ref`/`!include`** — follow external schema references (e.g. `common.yml`) that the current minimal YAML reader skips.
